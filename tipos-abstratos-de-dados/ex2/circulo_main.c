#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "circulo.h"

int main(void) {

    circ_cria(float x, float y, float r);
    circ_libera(Circulo *c);
    circ_area(Circulo *c);
    circ_interior(Circulo *c, Ponto *p);

    return 0;
}